# 更新记录

## 0.7.14

- 完整对比报告已由网页或 API 确认生成结束、正文充分且覆盖全部来源时，即使模型改写了标题或漏掉内部完成标记也会直接验收，不再为小格式差异整篇重问。
- 仍会拒绝正文过短、遗漏来源或中文报告语言异常等实质问题；必要时最多自动补全一次，并永久保存补全次数、原因和首次输出长度供诊断。
- 报告页会明确显示是否发生过自动补全，生成前说明也区分本地格式验收和真正的内容补全。
- 修复千问的宽泛消息列表选择器可能把多个旧问题、旧回答与当前回答一起采集的问题，只保留当前最后一条模型回答。

## 0.7.13

- 六家生产适配器统一采用有界完成协议：优先使用网站明确完成信号；信号缺失时，本轮新正文在非生成、非思考状态下连续 24 秒无任何字符变化后自动收录。
- 修复 Kimi 一次性挂载完整正文但不显示停止/重新生成控件时永久等待的问题；短答案同样可以自动收录，不再依赖字数或手动补采。
- 任何迟到字符和最后段落都会重置静默窗口；搜索计划、思考草稿和推理面板不会冒充最终正文。
- 原生输入在网页刚完成加载、偶发漏掉早期按键时，会在发送前安全清空并重输一次；每次均核对完整问题，校验不一致绝不发送。
- 诊断报告新增匿名化采集证据：正文长度、变化次数、稳定次数、生成状态和完成依据，不记录问题、回答、网址或账号信息。
- 新增可重复的真实站点验收与完整报告验收工具；六份测试原文可完整进入对比结论、HTML 和 Markdown 导出。

## 0.7.12

- 保留现有四宫格视觉，启动后先呈现程序外壳，再错峰加载四个网页；群发间隔 220 毫秒，降低瞬时卡顿。
- 模型状态改为“正在连接、发送、思考、输出、确认完整性、已收录”等自然语言，并加入轻量活动提示。
- 第一家回答完成后即可打开已有结果；每个模型可以单独重试或跳过，其他回答不受影响。
- 超时且已有正文时自动安全复查一次，不重复发送问题；失败后只显示当前最合适的恢复操作。
- 未发送的问题会自动保存在本机，支持 Ctrl+Enter；后台会诊和报告完成后通过托盘通知。
- 报告生成过程显示整理原文、提取观点、对齐分歧、核对来源等真实阶段，不再只显示笼统“生成中”。

## 0.7.11

- 修复豆包改版后的可信输入与回答定位：不再把输入框或用户问题误当回答，也不会被消息列表末尾的虚拟空白行卡住。
- 补采改为以用户确认和正文稳定性收口；即使网站残留失效的停止控件，也不会永久停在“补采中”。
- 六家模型共用同一套回答生命周期契约，并新增六家逐一覆盖的流式生成、稳定完成和原文完整性回归测试。
- 停止控件会排除 disabled、aria-disabled 和禁用样式；思考过程、推荐追问、操作栏及消息页脚不会混入原文。
- 可识别腾讯元宝的真实未登录页面，在面板和使用检查中明确显示“需要登录”，不再笼统表现为卡死。
- 新增六份完整原文生成一份可追溯完整对比报告的端到端测试，确保六个来源均进入报告并可回看原文。

## 0.7.10

- Kimi 在完成操作栏只有无文字图标时，会以多次正文变化、操作栏证据和 18 秒稳定窗口自动完成采集，不再要求手动补采。
- 原文采集只保留页面可见的最终回答，排除隐藏翻译、搜索过程和思考面板；当前回答仍在思考时也不会误取上一轮旧回答。
- 中文问题生成的完整对比报告强制使用简体中文；若英文正文比例异常则自动修复一次，避免中英混杂报告被当作完成结果。
- 增加 Kimi 完成态、思考内容隔离、旧答案隔离和报告语言质量的专项回归测试。

## 0.7.9

- 智谱在结束控件和“思考结束”均不可读时，可根据多次正文变化、正文长度和 18 秒稳定窗口自动完成采集；单条静止的思考/搜索计划仍不会冒充最终回答。
- 对比报告不再因为模型仅漏写内部结束标记而盲目再次生成：网页或 API 已确认结束且六个报告板块、全部来源均齐全时直接验收。
- 确实缺少结构或来源时最多自动修复一次，并持续显示失败原因与重试状态，不再表现为无响应后突然重生成。
- 报告窗口、HTML 标题、Markdown 标题和默认导出文件名统一为“完整对比报告”。

## 0.7.8

- 完整会诊报告和历史记录支持导出为自包含 HTML 或 Markdown，默认推荐手机友好的网页报告。
- 网页报告包含标题区、问题、目录、卡片式正文、横向滚动表格、打印样式和来源编号导航，不依赖外部样式或脚本。
- 导出时转义模型生成的原始 HTML，并限制外部链接协议，避免把回答中的脚本带入分享文件。

## 0.7.7

- 兼容智谱清言的新完成状态：操作栏只有图标时，以当前回答内的“思考结束”作为完成证据。
- 完成仍须同时满足正文发生变化、停止按钮消失、多次采样稳定，且“思考结束”后正文连续静默 10 秒，避免在思考暂停或最终正文流式输出时过早截断。
- 增加智谱旧回答/当前生成回答隔离和图标操作栏回归测试。

## 0.7.6

- 首次 GitHub 公开测试发行准备，项目原创源码采用 MIT。
- 增加隐私、安全、贡献、试用和公开源码审计工具。
- 精简打包过程：不收集应用没有使用的 QML 插件集合。
- 将第三方声明及匹配版本许可材料纳入发行检查。
- 会诊功能延续 0.7.5；不以更换版本号代替真实网站验收。

## 0.7.5

- 加强智谱和 Kimi 的完成判断，避免把中间搜索/思考计划当最终回答。
- 迟到回答支持显式补采；核对当前问题后才写入会诊。
- 未确认完整的材料保留可见，但不冒充完整来源参与结论。
- 报告针对更新后的来源刷新；综合处理中延后处理来源变化。
- 本次开源准备补充 MIT、隐私、安全、贡献、测试和发布说明及源码导出审计。

真实网站与第二台电脑验收仍待完成；本地自动化测试不代表全部模型始终可用。
